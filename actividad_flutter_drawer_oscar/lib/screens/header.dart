import 'package:flutter/material.dart';

AppBar header() {
  return AppBar(
    title: const Text('oscarluquee_'),
    backgroundColor: const Color.fromARGB(255, 255, 255, 255),
    foregroundColor: const Color.fromARGB(255, 5, 5, 5),
  );
}
